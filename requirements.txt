# =====================================================
# MCP Forge — зависимости
# =====================================================
# Установка:
#   python -m venv .venv
#   .venv\Scripts\activate        (Windows)
#   source .venv/bin/activate     (Linux / macOS)
#   pip install -r requirements.txt
# =====================================================

# ── Переменные окружения ────────────────────────────
python-dotenv>=1.0.0

# ── OpenAI-совместимый клиент ───────────────────────
# Используется для LM Studio, OpenAI и как транспорт для VseGPT
openai>=1.30.0

# ── HTTP-клиенты ────────────────────────────────────
# httpx — нужен lmstudio_client (load_model через /api/v0)
httpx>=0.27.0
# aiohttp — нужен vsegpt_client и _close_gui_tab в хосте
aiohttp>=3.9.0

# ── Gradio GUI ──────────────────────────────────────
gradio>=4.0.0

# ── Поиск DuckDuckGo ────────────────────────────────
ddgs>=6.0.0

# ── Утилиты ─────────────────────────────────────────
pyperclip>=1.8.0
pytz>=2024.1
qrcode[pil]>=7.4.2
pyyaml>=6.0.1

# ── Парсинг HTML ────────────────────────────────────
beautifulsoup4>=4.12.0
lxml>=5.0.0         # Ускоряет BeautifulSoup; без него используется html.parser

# ── Примечание ──────────────────────────────────────
# asyncio входит в стандартную библиотеку Python 3.4+
# и НЕ должен устанавливаться через pip.

# ── Опционально: интеграции из CHAT_INTEGRATION.md ──
# Раскомментировать по необходимости:
# aiogram>=3.0.0        # Telegram-бот (Способ 1)
# fastapi>=0.110.0      # REST API и OpenAI-прокси (Способы 2, 3)
# uvicorn>=0.29.0       # ASGI-сервер для FastAPI
